## To run:

1. Create virtual environment by running `python -m venv .venv`
2. Activate virtual environment by running `source .venv/bin/activate`
3. Install dependencies from requirements.txt using `pip install -r requirements.txt`
4. Run `python -m src.labor_allocation_project.bidder -h` to see available instances that can be ran.